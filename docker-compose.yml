services:
  redis:
    image: redis/redis-stack:latest
    container_name: hybrid-search-redis
    ports:
      - "6379:6379"
      - "8001:8001"
    restart: unless-stopped

models:
  embedding:
    model: docker.io/embeddinggemma:300M-Q8_0
    runtime_flags:
      - "--embeddings"